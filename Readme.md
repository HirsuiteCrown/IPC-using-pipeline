here we will see how interprocess communication happen using pipeline.

cat main.cpp | grep hello => this will return all line where hello is written in main.cpp as cat main.cpp simply print the whole code.

